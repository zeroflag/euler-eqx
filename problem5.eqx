\ https://projecteuler.net/problem=5

: divisible? ( num limit -- bool )
  1 + 2 do
    dup i % 0 != if drop false exit then
  loop
  drop true ;

: problem5 ( -- n )
  2520 begin dup 20 divisible? not while 20 + repeat ;

